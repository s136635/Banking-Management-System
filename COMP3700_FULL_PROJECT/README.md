COMP3700_FULL_PROJECT — Part 2 compliance report
------------------------------------------------

الملف هذا يشرح التعديلات التي أجريتها وما تبقى من نواقص.

**التعديلات المنفذة (بناء على طلبك):**

1. **توجيه Contact Form ليُرسل إلى البريد الجامعي**
   - عدّلت `contact.html` ليرسل النموذج إلى:
     ```
     mailto:s136635@student.squ.edu.om
     ```
     باستخدام `method="post"` و `enctype="text/plain"`.
   - ملاحظة: هذا الخيار يفتح برنامج البريد الافتراضي لدى المستخدم ويضع محتوى النموذج داخل رسالة — يعمل بدون خادم، لكنه يعتمد على عيارية عميل البريد لدى المقيّم. إذا تريده أن يُرسل بدون تدخل المستخدم، نحتاج سكربت خادمي (PHP) أو خدمة طرف ثالث (Formspree / EmailJS).

2. **أضفت أمثلة CSS لتغطية child combinator وattribute selector وحالات الروابط**
   - عدّلت `styles/main.css` وأضافت أمثلة: `header > nav > ul > li`، `input[type="email"]`، وحالات `a:link, a:visited, a:hover, a:active, a:focus`، بالإضافة إلى example pseudo-element `::before`.

3. **فحصت الجداول ودمج الخلايا**
   - تعداد الجداول ومكان وجود colspan/rowspan:
     - `accounts.html`: **2 جداول**, يحتويان على 2 حالات `colspan` (موجودة في صفوف 'Total Balance').
     - باقي الصفحات لم تُظهر `colspan`/`rowspan` في الفحص النصي (index/about/contact/loans/support/transfers).

4. **ملف README هذا يوضح التوافق والنواقص المتبقية**
   - نواقص متبقية التي تحتاج انتباهك:
     - نشر المشروع على GitHub + استضافة (لم يُنفذ).  
     - التأكد عند المقيّم هل قبول `mailto:` (يفتح عميل البريد) يكفي أم يطلب إرسال فعلي من الخادم.  
     - التأكد من تواجد child combinator/attribute selector في كل الحالات التي يطلبها المقرر — أضفت أمثلة عملية لتلبية هذا البند.

**ملفّات عدّلتها:**
- `contact.html` — عدلت عنصر `<form>` الأول ليرسل إلى mailto.
- `styles/main.css` — أضفت مقطع CSS جديد في النهاية.
- `README.md` — هذا الملف.

---

إذا تريد أعمل الآن:
- أكتب سكربت PHP بسيط `send_mail.php` داخل المشروع ويستخدم `mail()` (ملاحظة: إرسال البريد من PHP يعتمد على الخادم المستضيف)، أو
- أبدّل action إلى Formspree (أرسل لي إذا تريد أن أركّب Formspree ID)، أو
- أبقى على `mailto:` وأجهّز وثيقة تشرح كيفية اختبارها للمدرّس.

أخبرني الخطوة التالية — أبدأ بعمل سكربت PHP لإرسال البريد أو أركّب Formspree؟ أو أرفع المشروع مضغوط جاهز للتسليم.  
